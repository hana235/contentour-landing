-- ════════════════════════════════════════════════════════════════
-- 정산 중복 생성(이중 송금) 방지: 계약당 정산 1건 보장
-- 배경: 기존 이중 정산 방지는 브라우저 메모리 검사뿐이라, 동시/다중세션 처리 시
--       같은 contract_id로 43_정산내역이 2건 INSERT될 수 있었음(이중 송금 위험).
--       서버 액션(completeContract)에서 존재 검사 + 본 UNIQUE 제약으로 이중 방어.
-- ════════════════════════════════════════════════════════════════

-- 1) 적용 전 기존 중복 점검 (결과가 있으면 먼저 수동 정리 후 제약 추가)
--    SELECT contract_id, count(*) FROM "43_정산내역" GROUP BY contract_id HAVING count(*) > 1;

-- 2) 계약당 정산 1건 UNIQUE 제약 추가
ALTER TABLE "43_정산내역"
  ADD CONSTRAINT uq_43_정산내역_contract UNIQUE (contract_id);
